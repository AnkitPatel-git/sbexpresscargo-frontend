info:
  name: Product Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/product-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Product Master soft-delete by id. JWT + product_master_delete or product_master_all.
