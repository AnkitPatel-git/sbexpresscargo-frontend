info:
  name: State Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/state-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: State Master soft-delete by id. JWT + state_master_delete or state_master_all.
