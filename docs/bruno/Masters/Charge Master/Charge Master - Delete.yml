info:
  name: Charge Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/charge-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Charge Master soft-delete by id. JWT + charge_master_delete or charge_master_all.
