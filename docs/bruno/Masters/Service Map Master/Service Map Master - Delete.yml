info:
  name: Service Map Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/service-map-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Service Map Master soft-delete by id. JWT + service_map_master_delete or service_map_master_all.
