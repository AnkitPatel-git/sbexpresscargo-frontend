info:
  name: Bank Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/bank-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Bank Master soft-delete by id. JWT + bank_master_delete or bank_master_all.
