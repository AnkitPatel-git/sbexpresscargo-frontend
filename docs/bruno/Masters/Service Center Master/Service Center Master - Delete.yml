info:
  name: Service Center Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/service-center-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Service Center Master soft-delete by id. JWT + service_center_master_delete or service_center_master_all.
