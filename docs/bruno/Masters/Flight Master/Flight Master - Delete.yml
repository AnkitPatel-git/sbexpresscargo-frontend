info:
  name: Flight Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/flight-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Flight Master soft-delete by id. JWT + flight_master_delete or flight_master_all.
