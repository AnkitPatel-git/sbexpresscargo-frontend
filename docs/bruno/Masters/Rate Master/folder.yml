info:
  name: Rate Master
  type: folder
  seq: 17

request:
  auth: inherit

docs:
  content: "Rate master APIs aligned to `src/master/rate`: CRUD, export, calculation (`POST /rate-master/calculate`), preview (`POST /rate/preview`), and **List Zones** (`GET /zone-master`, same as `GET /rate-master/zones`). Full zone CRUD/import/export lives under **Masters → Zone Master**."
  type: text/markdown
