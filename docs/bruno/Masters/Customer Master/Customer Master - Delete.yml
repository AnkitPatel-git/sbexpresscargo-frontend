info:
  name: Customer Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/customer-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Customer Master soft-delete by id. JWT + customer_master_delete or customer_master_all.
