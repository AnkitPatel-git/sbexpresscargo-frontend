info:
  name: Serviceable Pincode
  type: folder
  seq: 21

request:
  auth: inherit

docs:
  content: |-
    Serviceable pincode CRUD and listing (`/utilities/serviceable-pincodes/*`). Alias admin pincode helpers: `GET /pincode`, `POST /pincode`. Implemented in `src/master/serviceable-pincode`; path kept under `/utilities/...` for backward compatibility.

    List/Search: roles with list permission; CRUD: Admin/Superuser (per backend guards).
  type: text/markdown
