info:
  name: Content Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/content-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Content Master soft-delete by id. JWT + content_master_delete or content_master_all.
