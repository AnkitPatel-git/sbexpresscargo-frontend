info:
  name: Consignee Master - Export (CSV)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/consignee-master/export"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseUrl}}/consignee-master/export"
      method: GET
      headers:
        - name: Authorization
          value: Bearer {{accessToken}}
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: text/csv
        - name: content-disposition
          value: 'attachment; filename="consignees.csv"'
      body:
        type: text
        data: "(empty CSV — no consignee records to export)"

docs: |-
  Export all Consignee Master records as a CSV file download. JWT + consignee_master_list or consignee_master_all.

  Response: text/csv download (Content-Disposition: attachment; filename="consignees.csv")
  Optional query params: same filters as the List endpoint (search, sortBy, sortOrder, etc.)
