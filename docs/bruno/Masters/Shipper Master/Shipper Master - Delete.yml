info:
  name: Shipper Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/shipper-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Shipper Master soft-delete by id. JWT + shipper_master_delete or shipper_master_all.
