info:
  name: Local Branch Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/local-branch-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Local Branch Master soft-delete by id. JWT + local_branch_master_delete or local_branch_master_all.
