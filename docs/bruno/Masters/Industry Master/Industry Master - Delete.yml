info:
  name: Industry Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/industry-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Industry Master soft-delete by id. JWT + industry_master_delete or industry_master_all.
