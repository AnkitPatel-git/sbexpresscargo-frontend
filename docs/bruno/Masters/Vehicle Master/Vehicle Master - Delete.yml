info:
  name: Vehicle Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/vehicle-master/{{vehicleId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
