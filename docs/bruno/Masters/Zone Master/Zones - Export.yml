info:
  name: Zones - Export
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/rate-master/zones/export?exportType=DOMESTIC"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Export zones with filters. JWT + master.zone.read.
