info:
  name: Zones - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/rate-master/zones/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete zone by id. JWT + master.zone.delete.
