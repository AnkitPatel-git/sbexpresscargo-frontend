info:
  name: Exception Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/exception-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Exception Master soft-delete by id. JWT + exception_master_delete or exception_master_all.
