info:
  name: Vendor Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/vendor-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Vendor Master soft-delete by id. JWT + vendor_master_delete or vendor_master_all.
