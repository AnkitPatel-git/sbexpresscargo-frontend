info:
  name: Carrier Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/carrier-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Soft-delete a carrier.

  **Path**

  | Segment | Description |
  |---------|-------------|
  | `:id`   | Carrier id |

  **Body:** none.

  JWT + `master.carrier.delete`
