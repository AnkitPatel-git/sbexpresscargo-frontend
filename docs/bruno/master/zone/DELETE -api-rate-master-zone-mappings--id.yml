info:
  name: DELETE /api/rate-master/zone-mappings/:id
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/api/rate-master/zone-mappings/:id"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
