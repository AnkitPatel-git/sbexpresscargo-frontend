info:
  name: PATCH /api/rate-master/zones/:id
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/rate-master/zones/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "countryId": 1,
        "version": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
