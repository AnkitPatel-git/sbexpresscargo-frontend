info:
  name: PUT /api/rate-master/zone-mappings/:id
  type: http
  seq: 12

http:
  method: PUT
  url: "{{baseUrl}}/api/rate-master/zone-mappings/:id"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "serviceablePincodeId": 2,
        "zoneId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
