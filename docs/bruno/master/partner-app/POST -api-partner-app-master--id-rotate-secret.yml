info:
  name: POST /api/partner-app-master/:id/rotate-secret
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/partner-app-master/1/rotate-secret"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
