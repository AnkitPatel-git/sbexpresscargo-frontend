info:
  name: PATCH /api/vehicle-master/:id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/vehicle-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "vehicleNo": "Sample Value",
        "vehicleType": "Sample Value",
        "ownerName": "Sample Name",
        "driverName": "Sample Name",
        "driverUserId": 1,
        "capacityKg": 1,
        "status": "ACTIVE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
