info:
  name: PUT /api/country-master/:id
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/country-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "code": "CODE001",
        "name": "Sample Name",
        "weightUnit": 10,
        "currency": "Sample Value",
        "isdCode": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
