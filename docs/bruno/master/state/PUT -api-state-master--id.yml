info:
  name: PUT /api/state-master/:id
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/state-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "countryId": 1,
        "stateName": "Sample Name",
        "stateCode": "MH",
        "gstAlias": "27ABCDE1234F1Z5",
        "unionTerritory": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
