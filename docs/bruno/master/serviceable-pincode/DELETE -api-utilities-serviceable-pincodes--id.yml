info:
  name: DELETE /api/utilities/serviceable-pincodes/:id
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/api/utilities/serviceable-pincodes/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
