info:
  name: PATCH /api/vendor-config-master/:id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/vendor-config-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "vendorId": 1,
        "serviceMapId": 1,
        "environment": "Sample Value",
        "customerId": 1,
        "apiKey": "Sample Value",
        "secretKey": "Sample Value",
        "baseUrl": "https://example.com",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
