info:
  name: PUT /api/bank-master/:id
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/bank-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "bankCode": "CODE001",
        "bankName": "Sample Name",
        "status": "ACTIVE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
