info:
  name: POST /api/city-master
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/city-master"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "countryId": 1,
        "stateId": 1,
        "cityName": "DEWASH"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
