info:
  name: PUT /api/exception-master/:id
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/exception-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "code": "CODE001",
        "name": "Sample Name",
        "type": "Sample Value",
        "inscan": true,
        "showOnMobileApps": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
