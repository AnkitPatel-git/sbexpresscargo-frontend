info:
  name: PATCH /api/customer-master/:customerId/kyc-documents/:docId
  type: http
  seq: 18

http:
  method: PATCH
  url: "{{baseUrl}}/api/customer-master/{{customerId}}/kyc-documents/{{docId}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: customerId
      value: "1"
      type: path
    - name: docId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "docType": "AADHAR",
        "filePath": "Sample Value",
        "fileName": "Sample Name",
        "documentNumber": "Sample Value",
        "expiryDate": "2026-04-13T10:00:00.000Z",
        "verified": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
