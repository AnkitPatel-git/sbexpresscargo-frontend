info:
  name: POST /api/customer-master/:customerId/other-charges
  type: http
  seq: 25

http:
  method: POST
  url: "{{baseUrl}}/api/customer-master/{{customerId}}/other-charges"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: customerId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "vendorId": 1,
        "productId": 1,
        "srNo": 1,
        "chargeType": "FIXED",
        "fromDate": "2026-04-13T10:00:00.000Z",
        "toDate": "2026-04-13T10:00:00.000Z",
        "origin": "Sample Value",
        "destination": "Sample Value",
        "amount": 10,
        "minimumValue": 1
      }
  auth: inherit

runtime:
  variables:
    - name: customerId
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
