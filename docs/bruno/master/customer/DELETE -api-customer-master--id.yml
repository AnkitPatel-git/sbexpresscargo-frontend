info:
  name: DELETE /api/customer-master/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/customer-master/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
