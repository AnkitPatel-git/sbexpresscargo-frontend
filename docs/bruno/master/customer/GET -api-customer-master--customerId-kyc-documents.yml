info:
  name: GET /api/customer-master/:customerId/kyc-documents
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/customer-master/{{customerId}}/kyc-documents"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: customerId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
