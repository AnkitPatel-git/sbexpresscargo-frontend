info:
  name: GET /api/customer-master/:customerId/volumetrics/:volumetricId
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/customer-master/{{customerId}}/volumetrics/{{volumetricId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: customerId
      value: "1"
      type: path
    - name: volumetricId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
