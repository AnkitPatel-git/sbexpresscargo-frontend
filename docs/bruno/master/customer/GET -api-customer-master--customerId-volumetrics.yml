info:
  name: GET /api/customer-master/:customerId/volumetrics
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/customer-master/{{customerId}}/volumetrics"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: customerId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
