info:
  name: DELETE /api/customer-master/:customerId/volumetrics/:volumetricId
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/customer-master/{{customerId}}/volumetrics/{{volumetricId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: customerId
      value: "1"
      type: path
    - name: volumetricId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
