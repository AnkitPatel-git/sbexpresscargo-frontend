info:
  name: Delete Vendor Status Mapping
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/vendor-status-mappings/{{vendorStatusMappingId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
