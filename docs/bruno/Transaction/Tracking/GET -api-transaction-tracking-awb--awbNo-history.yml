info:
  name: GET /api/transaction/tracking/awb/:awbNo/history
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/tracking/awb/{{awbNo}}/history"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: format
      value: string
      type: query
    - name: awbNo
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
