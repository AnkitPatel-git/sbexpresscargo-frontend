info:
  name: PATCH /api/transaction/tracking/events/:id
  type: http
  seq: 10

http:
  method: PATCH
  url: "{{baseUrl}}/api/transaction/tracking/events/1"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |
      {
        "scannedAt": "2026-07-04T10:30:00+05:30"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
