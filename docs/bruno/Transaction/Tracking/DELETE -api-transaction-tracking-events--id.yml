info:
  name: DELETE /api/transaction/tracking/events/:id
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/api/transaction/tracking/events/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
