info:
  name: POST /api/transaction/tracking/retry-failed
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/tracking/retry-failed"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: '"Sample Value"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
