info:
  name: GET /api/transaction/tracking/metrics
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/tracking/metrics"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
