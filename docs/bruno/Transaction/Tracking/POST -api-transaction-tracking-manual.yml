info:
  name: POST /api/transaction/tracking/manual
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/tracking/manual"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: '"Sample Value"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
