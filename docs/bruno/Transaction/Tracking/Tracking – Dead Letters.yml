info:
  name: Tracking – Dead Letters
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/transaction/tracking/dead-letters?limit=100"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Dead-letter vendor tracking logs. JWT + tracking_view or tracking_all.
