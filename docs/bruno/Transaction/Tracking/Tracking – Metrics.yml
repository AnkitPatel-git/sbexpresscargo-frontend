info:
  name: Tracking – Metrics
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/transaction/tracking/metrics"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Tracking engine metrics. JWT + tracking_view or tracking_all.
