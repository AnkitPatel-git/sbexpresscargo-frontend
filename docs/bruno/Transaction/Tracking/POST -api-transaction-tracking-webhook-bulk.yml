info:
  name: POST /api/transaction/tracking/webhook/bulk
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/tracking/webhook/bulk"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: '"Sample Value"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
