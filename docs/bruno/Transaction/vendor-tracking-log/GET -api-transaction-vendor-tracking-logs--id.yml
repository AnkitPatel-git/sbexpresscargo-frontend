info:
  name: GET /api/transaction/vendor-tracking-logs/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/vendor-tracking-logs/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
