info:
  name: Delete Tracking Summary
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/tracking-summary/{{trackingSummaryShipmentId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
