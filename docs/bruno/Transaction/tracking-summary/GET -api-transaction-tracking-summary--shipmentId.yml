info:
  name: GET /api/transaction/tracking-summary/:shipmentId
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/tracking-summary/{{shipmentId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: shipmentId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
