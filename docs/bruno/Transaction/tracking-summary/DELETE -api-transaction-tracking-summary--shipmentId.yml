info:
  name: DELETE /api/transaction/tracking-summary/:shipmentId
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/api/transaction/tracking-summary/{{shipmentId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: shipmentId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
