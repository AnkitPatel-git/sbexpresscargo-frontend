info:
  name: List Misrouted Scans
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/transaction/misrouted-scan?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List misrouted scans. JWT + event_scan_list/search/all.
