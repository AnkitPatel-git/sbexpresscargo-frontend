info:
  name: POST /api/transaction/shipment/:id/status
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/shipment/{{id}}/status"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "ACTIVE",
        "version": 1,
        "reason": "Sample Value"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
