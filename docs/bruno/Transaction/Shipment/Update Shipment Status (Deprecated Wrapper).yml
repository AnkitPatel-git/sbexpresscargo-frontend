info:
  name: Update Shipment Status (Deprecated Wrapper)
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/transaction/shipment/2/status"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "version": 1,
        "status": "PICKED",
        "reason": "Manual override"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "DEPRECATED: routes to TrackingProcessorService.addManualStatus internally. Prefer tracking APIs."
