info:
  name: POST /api/transaction/shipment/:id/pod
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/shipment/{{id}}/pod"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: '"Sample Value"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
