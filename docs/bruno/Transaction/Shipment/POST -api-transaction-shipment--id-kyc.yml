info:
  name: POST /api/transaction/shipment/:id/kyc
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/shipment/{{id}}/kyc"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "type": "Sample Value",
        "entryType": "Sample Value",
        "entryDate": "2026-04-13T10:00:00.000Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
