info:
  name: Delete Undelivered Scan
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/undelivered-scan/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete undelivered scan. JWT + event_scan_delete or event_scan_all.
