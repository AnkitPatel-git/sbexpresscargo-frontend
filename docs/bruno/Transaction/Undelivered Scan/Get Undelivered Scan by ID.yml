info:
  name: Get Undelivered Scan by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/transaction/undelivered-scan/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get undelivered scan details. JWT + event_scan_list/search/all.
