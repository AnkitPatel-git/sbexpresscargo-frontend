info:
  name: Export Undelivered Scans (CSV)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/transaction/undelivered-scan/export"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Export undelivered scans as CSV. JWT + event_scan_list/search/all.
