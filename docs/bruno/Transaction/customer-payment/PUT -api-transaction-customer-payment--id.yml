info:
  name: PUT /api/transaction/customer-payment/:id
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/transaction/customer-payment/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "date": "2026-04-13T10:00:00.000Z",
        "paidDate": "2026-04-13T10:00:00.000Z",
        "amount": 10,
        "remark": "Sample note",
        "customerId": 1,
        "invoiceNo": "Sample Value",
        "approved": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
