info:
  name: List DRS
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/transaction/drs?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List DRS headers. JWT + drs_list/search/all.
