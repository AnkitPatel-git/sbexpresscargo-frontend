info:
  name: Export DRS (CSV)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/transaction/drs/export"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Export DRS headers as CSV. JWT + drs_list/search/all.
