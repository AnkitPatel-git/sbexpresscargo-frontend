info:
  name: List Manifest Inscans
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/transaction/manifest/inscan?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List manifest inscans. JWT + manifest_list/search/all.
