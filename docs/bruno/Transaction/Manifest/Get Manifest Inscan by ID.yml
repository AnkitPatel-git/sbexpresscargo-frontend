info:
  name: Get Manifest Inscan by ID
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseUrl}}/transaction/manifest/inscan/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Manifest inscan details with items. JWT + manifest_list/search/all.
