info:
  name: DELETE /api/transaction/manifest/:id/shipments/:shipmentId
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/transaction/manifest/{{id}}/shipments/{{shipmentId}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
    - name: shipmentId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "version": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
