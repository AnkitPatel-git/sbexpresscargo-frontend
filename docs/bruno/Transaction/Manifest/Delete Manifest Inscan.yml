info:
  name: Delete Manifest Inscan
  type: http
  seq: 21

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/manifest/inscan/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete manifest inscan. JWT + manifest_delete or manifest_all.
