info:
  name: Delete Manifest
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/manifest/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete manifest. JWT + manifest_delete or manifest_all.
