info:
  name: GET /api/transaction/manifest/next-number
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/manifest/next-number"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: serviceCenterId
      value: string
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
