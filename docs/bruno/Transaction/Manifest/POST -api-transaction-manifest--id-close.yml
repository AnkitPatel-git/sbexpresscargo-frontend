info:
  name: POST /api/transaction/manifest/:id/close
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/manifest/{{id}}/close"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "version": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
