info:
  name: GET /api/transaction/manifest/:id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/manifest/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
