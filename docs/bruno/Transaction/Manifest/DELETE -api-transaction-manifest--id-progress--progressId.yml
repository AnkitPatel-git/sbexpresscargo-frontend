info:
  name: DELETE /api/transaction/manifest/:id/progress/:progressId
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/transaction/manifest/{{id}}/progress/{{progressId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
    - name: progressId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
