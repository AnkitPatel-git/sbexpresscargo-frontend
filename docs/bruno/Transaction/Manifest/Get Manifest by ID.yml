info:
  name: Get Manifest by ID
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/transaction/manifest/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Manifest details with items and progress. JWT + manifest_list/search/all.
