info:
  name: GET /api/transaction/undelivered-scan/:id
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/undelivered-scan/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
