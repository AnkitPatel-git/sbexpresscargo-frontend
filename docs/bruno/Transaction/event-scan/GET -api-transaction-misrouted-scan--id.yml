info:
  name: GET /api/transaction/misrouted-scan/:id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/misrouted-scan/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
