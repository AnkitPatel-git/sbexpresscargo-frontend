info:
  name: GET /api/transaction/pod/example
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/transaction/pod/example"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
