info:
  name: POST /api/transaction/pod/view
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/transaction/pod/view"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "awbNos": [
          "AWB100001"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
