info:
  name: POD – Export Excel
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/transaction/pod/export?awbNos=AWB123456,AWB123457"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Export POD Excel for AWB list. JWT + pod_download or pod_all.
