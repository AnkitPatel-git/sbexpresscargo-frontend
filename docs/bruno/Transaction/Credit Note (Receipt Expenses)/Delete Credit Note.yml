info:
  name: Delete Credit Note
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/receipt-expenses/credit-note/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Cancel/soft-delete credit note. JWT + credit_note_delete or credit_note_all.
