info:
  name: GET /api/dashboard/sales
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard/sales"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: fromDate
      value: string
      type: query
    - name: toDate
      value: string
      type: query
    - name: serviceCenterId
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
