info:
  name: GET /api/dashboard/sales/service-centers
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard/sales/service-centers"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: year
      value: "0"
      type: query
    - name: month
      value: "0"
      type: query
    - name: customerId
      value: "0"
      type: query
    - name: serviceCenterId
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
