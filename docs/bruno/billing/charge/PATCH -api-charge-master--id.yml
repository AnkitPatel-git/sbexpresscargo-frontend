info:
  name: PATCH /api/charge-master/:id
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/charge-master/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "code": "CODE001",
        "name": "Sample Name",
        "calculationBase": "FLAT",
        "applyFuel": true,
        "sequence": 1,
        "version": 1
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
