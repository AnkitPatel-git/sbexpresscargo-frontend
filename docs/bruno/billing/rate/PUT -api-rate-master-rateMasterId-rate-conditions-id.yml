info:
  name: PUT /api/rate-master/:rateMasterId/rate-conditions/:id
  type: http
  seq: 25

http:
  method: PUT
  url: "{{baseUrl}}/api/rate-master/{{rateMasterId}}/rate-conditions/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: rateMasterId
      value: "1"
      type: path
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "field": "WEIGHT",
        "operator": "GT",
        "value": 15,
        "chargeName": "ODD_SIZE",
        "chargeAmount": 60,
        "isPercentage": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
