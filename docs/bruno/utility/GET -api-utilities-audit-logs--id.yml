info:
  name: GET /api/utilities/audit-logs/:id
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/utilities/audit-logs/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
