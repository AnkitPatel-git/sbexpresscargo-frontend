info:
  name: PATCH /api/utilities/roles/:id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/utilities/roles/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "string",
        "identifier": "string",
        "description": "string",
        "permissionIds": [
          0
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
