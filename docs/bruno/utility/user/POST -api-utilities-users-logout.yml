info:
  name: POST /api/utilities/users/logout
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/utilities/users/logout"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sample": "value",
        "context": {
          "pathParams": {},
          "query": {}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
