info:
  name: PATCH /api/utilities/users/:id/status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/utilities/users/{{id}}/status"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
