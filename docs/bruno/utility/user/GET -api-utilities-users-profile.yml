info:
  name: GET /api/utilities/users/profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/utilities/users/profile"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
