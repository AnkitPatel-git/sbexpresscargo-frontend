info:
  name: GET /api/utilities/users/roles
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/utilities/users/roles"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
