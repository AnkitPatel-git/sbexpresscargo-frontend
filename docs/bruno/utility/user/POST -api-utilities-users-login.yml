info:
  name: POST /api/utilities/users/login
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/utilities/users/login"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "email": "admin@system.com",
        "password": "Admin@123",
        "platform": "portal"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
