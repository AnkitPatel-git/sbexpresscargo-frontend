info:
  name: DELETE /api/utilities/permissions/:id
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/api/utilities/permissions/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
