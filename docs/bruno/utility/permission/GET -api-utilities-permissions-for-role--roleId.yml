info:
  name: GET /api/utilities/permissions/for-role/:roleId
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/utilities/permissions/for-role/{{roleId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: roleId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
