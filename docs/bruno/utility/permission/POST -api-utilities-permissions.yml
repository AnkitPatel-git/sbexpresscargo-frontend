info:
  name: POST /api/utilities/permissions
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/utilities/permissions"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "name": "string",
        "underMenu": "string",
        "description": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
