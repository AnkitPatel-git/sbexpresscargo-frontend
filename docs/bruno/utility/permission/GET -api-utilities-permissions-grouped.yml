info:
  name: GET /api/utilities/permissions/grouped
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/utilities/permissions/grouped"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
