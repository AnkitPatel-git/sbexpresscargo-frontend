info:
  name: DELETE /api/utilities/permissions/remove
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/utilities/permissions/remove"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: roleId
      value: string
      type: query
    - name: permissionId
      value: string
      type: query
  body:
    type: json
    data: |-
      {
        "roleId": 0,
        "permissionId": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
