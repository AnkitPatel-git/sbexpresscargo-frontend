info:
  name: PUT /api/utilities/permissions/:id
  type: http
  seq: 9

http:
  method: PUT
  url: "{{baseUrl}}/api/utilities/permissions/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "string",
        "underMenu": "string",
        "description": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
