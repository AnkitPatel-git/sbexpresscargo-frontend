info:
  name: Tax Setup - Delete
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/tax-charges/tax-setup/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete tax setup. JWT + tax_setup_delete/all.
