info:
  name: Rate Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/rate-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete rate master. JWT + rate_master_delete/all.
