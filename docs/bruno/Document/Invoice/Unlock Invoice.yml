info:
  name: Unlock Invoice
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/document/invoice/1/unlock"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Unlock invoice. JWT + invoice.core.create.
