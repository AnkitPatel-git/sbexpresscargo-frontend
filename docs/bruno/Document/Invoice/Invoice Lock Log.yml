info:
  name: Invoice Lock Log
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/document/invoice/lock-log"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get invoice lock/unlock audit log. JWT + invoice.core.read.
