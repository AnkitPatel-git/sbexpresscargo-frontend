info:
  name: Export Invoices (CSV)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/document/invoice/export"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Export invoices as CSV. JWT + invoice.core.read.
