info:
  name: POST /api/document/invoice/:id/unlock
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/document/invoice/{{id}}/unlock"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "sample": "value",
        "context": {
          "pathParams": {
            "id": "1"
          },
          "query": {}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
