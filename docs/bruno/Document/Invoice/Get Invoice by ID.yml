info:
  name: Get Invoice by ID
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/document/invoice/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get invoice details. JWT + invoice.core.read.
