info:
  name: Invoice Print Data
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/document/invoice/print?invoiceIds=1,2,3"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get print payload for invoices. JWT + invoice.core.read.
