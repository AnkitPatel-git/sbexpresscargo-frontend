info:
  name: POST /api/document/invoice/preview
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/document/invoice/preview"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "year": "2025-26",
        "fromDate": "2025-04-01",
        "toDate": "2026-03-31",
        "productType": "DOMESTIC",
        "serviceCenterId": 1,
        "billingType": "PREPAID",
        "registerType": "REGISTERED",
        "customerId": 1,
        "showAwb": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
