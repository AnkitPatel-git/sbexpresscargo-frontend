info:
  name: List Invoices
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/document/invoice?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List/search invoices. JWT + invoice.core.read.
