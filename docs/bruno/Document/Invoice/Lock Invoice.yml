info:
  name: Lock Invoice
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/document/invoice/1/lock"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lock invoice. JWT + invoice.core.create.
