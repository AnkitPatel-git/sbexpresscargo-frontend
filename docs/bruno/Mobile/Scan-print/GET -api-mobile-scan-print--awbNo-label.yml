info:
  name: GET /api/mobile/scan-print/:awbNo/label
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/scan-print/{{awbNo}}/label"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
    - name: Accept
      value: application/pdf
      disabled: true
  params:
    - name: awbNo
      value: "AWB100001"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
