info:
  name: Mark one read
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/mobile/notifications/{{mobileNotificationId}}/read"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.notification.read
