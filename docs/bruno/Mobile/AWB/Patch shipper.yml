info:
  name: Patch shipper
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/mobile/awb/{{mobileAwbNo}}/shipper"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.awb.update
