info:
  name: PATCH /api/mobile/awb/:awbNo/shipper
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/mobile/awb/{{awbNo}}/shipper"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: awbNo
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "shipperName": "string",
        "shipperOrigin": "string",
        "shipperCode": "string",
        "address1": "string",
        "address2": "string",
        "city": "string",
        "state": "string",
        "telephone": "string",
        "mobile": "string",
        "email": "string",
        "gstNo": "string",
        "panNo": "string",
        "invoiceNo": "string",
        "invoiceDate": "string",
        "currency": "string",
        "termOfInvoice": "string",
        "exportReason": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
