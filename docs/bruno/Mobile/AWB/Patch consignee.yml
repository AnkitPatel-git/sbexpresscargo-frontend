info:
  name: Patch consignee
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/mobile/awb/{{mobileAwbNo}}/consignee"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.awb.update
