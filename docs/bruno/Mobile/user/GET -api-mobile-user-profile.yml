info:
  name: GET /api/mobile/user/profile
  type: http
  seq: 99

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/user/profile"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
