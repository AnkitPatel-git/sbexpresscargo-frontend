info:
  name: GET /api/mobile/tracking/search
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/tracking/search"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: q
      value: string
      type: query
    - name: page
      value: string
      type: query
    - name: limit
      value: string
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
