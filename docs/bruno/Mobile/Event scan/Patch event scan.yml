info:
  name: Patch event scan
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/mobile/event-scan/1"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.event_scan.update
