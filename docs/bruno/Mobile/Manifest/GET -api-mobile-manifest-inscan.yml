info:
  name: GET /api/mobile/manifest/inscan
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/manifest/inscan"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: date
      value: string
      type: query
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
