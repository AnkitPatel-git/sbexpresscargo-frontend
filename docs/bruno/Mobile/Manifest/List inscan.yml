info:
  name: List inscan
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/mobile/manifest/inscan?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.manifest.read
