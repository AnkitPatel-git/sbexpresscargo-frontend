info:
  name: List manifests
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/mobile/manifest?page=1&limit=20&status="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.manifest.read
