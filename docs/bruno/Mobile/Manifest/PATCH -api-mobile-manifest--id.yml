info:
  name: PATCH /api/mobile/manifest/:id
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/mobile/manifest/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "manifestNo": "string",
        "vendorId": 0,
        "location": "string",
        "connectStation": "string",
        "version": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
