info:
  name: POST /api/mobile/manifest/:id/awb
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/mobile/manifest/{{id}}/awb"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "awbNo": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
