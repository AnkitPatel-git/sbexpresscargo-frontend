info:
  name: POST /api/mobile/manifest/inscan
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/mobile/manifest/inscan"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "awbNos": [
          "string"
        ],
        "serviceCenterId": 0,
        "inscanAt": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
