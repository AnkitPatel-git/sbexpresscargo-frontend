info:
  name: GET /api/mobile/manifest
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/manifest"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: status
      value: CREATED
      type: query
    - name: search
      value: string
      type: query
    - name: date
      value: string
      type: query
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
