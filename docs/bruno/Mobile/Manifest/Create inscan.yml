info:
  name: Create inscan
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/mobile/manifest/inscan"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "manifestNo": "INS-001",
        "manifestDate": "2026-03-20",
        "awbNos": ["AWB001"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.manifest.update
