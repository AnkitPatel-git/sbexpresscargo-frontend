info:
  name: Mobile
  type: folder
  seq: 10

request:
  auth: inherit

docs:
  content: 'Mobile field APIs. Token: login with `platform: "mobile"`. Requires `mobileAppLens` on user profile. Permission identifiers: mobile.*'
  type: text/markdown
