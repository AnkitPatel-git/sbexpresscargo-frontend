info:
  name: Couriers
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/mobile/lookup/couriers?search="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.lookup.read
