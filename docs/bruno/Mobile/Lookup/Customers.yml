info:
  name: Customers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/mobile/lookup/customers?search="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.lookup.read
