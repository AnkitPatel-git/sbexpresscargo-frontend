info:
  name: GET /api/mobile/lookup/products
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/lookup/products?page=1&limit=50&search="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "50"
      type: query
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: list item
    request:
      url: "{{baseUrl}}/api/mobile/lookup/products"
      method: GET
      headers:
        - name: Authorization
          value: Bearer {{accessToken}}
    response:
      status: 200
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Success",
            "data": {
              "items": [
                {
                  "id": 1,
                  "productCode": "SUR",
                  "productName": "Surface",
                  "weightUnit": "KG"
                }
              ],
              "total": 1,
              "page": 1,
              "limit": 50
            }
          }
