info:
  name: GET /api/mobile/lookup/shipment-tracking-statuses
  type: http
  seq: 99

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/lookup/shipment-tracking-statuses"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
