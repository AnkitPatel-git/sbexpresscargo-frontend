info:
  name: GET /api/mobile/lookup/areas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/lookup/areas?page=1&limit=50"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "50"
      type: query
    - name: serviceCenterId
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
