info:
  name: Areas
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/mobile/lookup/areas?search="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.lookup.read
