info:
  name: Products
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/mobile/lookup/products?search="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.lookup.read
