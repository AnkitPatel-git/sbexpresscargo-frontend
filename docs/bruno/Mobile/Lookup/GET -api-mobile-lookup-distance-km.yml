info:
  name: GET /api/mobile/lookup/distance-km
  type: http
  seq: 99

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/lookup/distance-km?fromPincode=452001&toPincode=400001"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
