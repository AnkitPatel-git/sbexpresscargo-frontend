info:
  name: GET /api/mobile/lookup/payment-types
  type: http
  seq: 99

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/lookup/payment-types"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: values
    request:
      url: "{{baseUrl}}/api/mobile/lookup/payment-types"
      method: GET
      headers:
        - name: Authorization
          value: Bearer {{accessToken}}
    response:
      status: 200
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Success",
            "data": {
              "items": [
                { "value": "CREDIT", "label": "Credit" },
                { "value": "CASH", "label": "Cash" },
                { "value": "TO_PAY", "label": "COD/To Pay" }
              ]
            }
          }
