info:
  name: GET /api/mobile/lookup/exceptions
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/lookup/exceptions?page=1&limit=50"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
