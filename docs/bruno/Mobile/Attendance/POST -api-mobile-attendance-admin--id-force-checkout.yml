info:
  name: POST /api/mobile/attendance-admin/:id/force-checkout
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/mobile/attendance-admin/{{id}}/force-checkout"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "remarks": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
