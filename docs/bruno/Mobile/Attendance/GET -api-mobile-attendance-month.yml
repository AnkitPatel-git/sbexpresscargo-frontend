info:
  name: GET /api/mobile/attendance/month
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/attendance/month"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: year
      value: "0"
      type: query
    - name: month
      value: "0"
      type: query
    - name: userId
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
