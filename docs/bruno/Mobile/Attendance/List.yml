info:
  name: List
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/mobile/attendance?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "mobile.attendance.read — default last 30 days (IST). Optional fromDate, toDate (both required if used; max 90 days). SUPER_ADMIN: optional userId, serviceCenterId."
