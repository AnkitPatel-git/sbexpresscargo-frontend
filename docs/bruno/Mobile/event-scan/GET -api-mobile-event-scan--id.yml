info:
  name: GET /api/mobile/event-scan/:id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/event-scan/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: eventType
      value: string
      type: query
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
