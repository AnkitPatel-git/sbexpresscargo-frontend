info:
  name: POST /api/mobile/event-scan
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/mobile/event-scan"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "eventType": "string",
        "scanAt": "string",
        "serviceCenterId": 0,
        "awbNos": [
          "string"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
