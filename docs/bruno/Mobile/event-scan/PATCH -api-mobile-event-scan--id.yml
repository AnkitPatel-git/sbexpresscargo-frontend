info:
  name: PATCH /api/mobile/event-scan/:id
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/api/mobile/event-scan/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "scanAt": "string",
        "serviceCenterId": 0,
        "awbNos": [
          "string"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
