info:
  name: GET /api/mobile/pod/awb/:awbNo
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/pod/awb/{{awbNo}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: awbNo
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
