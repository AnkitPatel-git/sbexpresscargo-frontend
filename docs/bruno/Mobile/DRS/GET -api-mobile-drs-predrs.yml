info:
  name: GET /api/mobile/drs/predrs
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/drs/predrs"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: serviceCenterId
      value: "0"
      type: query
    - name: limit
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
