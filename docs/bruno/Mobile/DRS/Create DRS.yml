info:
  name: Create DRS
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/mobile/drs"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "drsNo": "DRS-001",
        "drsAt": "2026-03-20T10:00:00.000Z",
        "awbNos": ["AWB001"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.drs.create
