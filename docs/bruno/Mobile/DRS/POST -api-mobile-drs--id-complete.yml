info:
  name: POST /api/mobile/drs/:id/complete
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/mobile/drs/{{id}}/complete"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "version": 0,
        "items": [
          {
            "awbNo": "string",
            "status": "DELIVERED",
            "remark": "string",
            "receiverName": "string",
            "exceptionId": 0
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
