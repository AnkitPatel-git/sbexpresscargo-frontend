info:
  name: GET /api/mobile/drs/:id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/drs/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
