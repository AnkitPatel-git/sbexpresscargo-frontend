info:
  name: POST /api/mobile/drs
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/mobile/drs"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "drsNo": "string",
        "drsAt": "string",
        "courierId": 0,
        "serviceCenterId": 0,
        "remark": "string",
        "awbNos": [
          "string"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
