info:
  name: GET /api/mobile/drs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/mobile/drs"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  params:
    - name: status
      value: CREATED
      type: query
    - name: page
      value: "0"
      type: query
    - name: limit
      value: "0"
      type: query
    - name: date
      value: string
      type: query
    - name: serviceCenterId
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
