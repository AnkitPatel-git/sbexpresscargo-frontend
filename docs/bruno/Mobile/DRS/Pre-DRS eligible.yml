info:
  name: Pre-DRS eligible
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/mobile/drs/predrs?limit=200"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.drs.read
