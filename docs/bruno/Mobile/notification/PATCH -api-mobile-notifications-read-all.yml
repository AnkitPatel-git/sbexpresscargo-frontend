info:
  name: PATCH /api/mobile/notifications/read-all
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/api/mobile/notifications/read-all"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sample": "value",
        "context": {
          "pathParams": {},
          "query": {}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
