info:
  name: PATCH /api/mobile/notifications/:id/read
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/mobile/notifications/{{id}}/read"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "sample": "value",
        "context": {
          "pathParams": {
            "id": "1"
          },
          "query": {}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
