info:
  name: GET /api
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
