info:
  name: Rate Master - Get by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/rate-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get one rate master by id. JWT + master.rate.read.
