info:
  name: Area Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/area-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Area Master soft-delete by id. JWT + area_master_delete or area_master_all.
