info:
  name: Country Master - Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/country-master/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Country Master soft-delete by id. JWT + country_master_delete or country_master_all.
