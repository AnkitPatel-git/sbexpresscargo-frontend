info:
  name: Delete Receipt
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/receipt/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
