info:
  name: List Vendor Status Mappings
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/transaction/vendor-status-mappings?page=1&limit=20&vendorId=1&search=OUT"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
