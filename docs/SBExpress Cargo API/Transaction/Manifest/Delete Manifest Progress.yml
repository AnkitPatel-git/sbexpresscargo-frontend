info:
  name: Delete Manifest Progress
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/manifest/1/progress/1"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete manifest progress row. JWT + manifest_modify or manifest_all.
