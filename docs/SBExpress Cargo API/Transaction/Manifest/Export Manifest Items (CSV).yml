info:
  name: Export Manifest Items (CSV)
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/transaction/manifest/1/export"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Export manifest items as CSV. JWT + manifest_list/search/all.
