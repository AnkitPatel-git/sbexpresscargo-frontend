info:
  name: Manifest View (Aggregated)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/transaction/manifest/view?dateFrom=&dateTo=&origin=&destination=&vendor=&search=&searchBy="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Aggregated manifest view with shipment count and weight. JWT + manifest_list/search/all.
