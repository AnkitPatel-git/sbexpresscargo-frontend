info:
  name: Get Tracking Summary by Shipment ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/transaction/tracking-summary/{{trackingSummaryShipmentId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
