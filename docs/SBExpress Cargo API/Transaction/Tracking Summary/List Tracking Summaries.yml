info:
  name: List Tracking Summaries
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/transaction/tracking-summary?page=1&limit=20&shipmentId=1&awbNo=AWB"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
