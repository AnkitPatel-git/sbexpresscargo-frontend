info:
  name: Start DRS
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/transaction/drs/1/start"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Mark all DRS shipments OFD via tracking engine. JWT + drs_modify or drs_all.
