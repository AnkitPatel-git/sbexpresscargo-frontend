info:
  name: Tracking – History (CSV)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/transaction/tracking/awb/AWB123456/history?format=csv"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Download status history as CSV. JWT + tracking_view or tracking_all.
