info:
  name: List Credit Notes
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/transaction/receipt-expenses/credit-note?page=1&limit=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List/search credit notes. JWT + credit_note_list/search/all.
