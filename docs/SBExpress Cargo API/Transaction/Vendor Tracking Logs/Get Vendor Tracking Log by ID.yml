info:
  name: Get Vendor Tracking Log by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/transaction/vendor-tracking-logs/{{vendorTrackingLogId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
