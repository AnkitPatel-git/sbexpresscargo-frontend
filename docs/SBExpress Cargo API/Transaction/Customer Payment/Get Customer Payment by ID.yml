info:
  name: Get Customer Payment by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/transaction/customer-payment/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a single customer payment. JWT + customer_payment_list or customer_payment_all.
