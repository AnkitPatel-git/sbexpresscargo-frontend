info:
  name: Patch receipt
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/mobile/awb/{{mobileAwbNo}}/receipt"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.awb.update
