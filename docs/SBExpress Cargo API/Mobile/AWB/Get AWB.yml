info:
  name: Get AWB
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/mobile/awb/{{mobileAwbNo}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.awb.read
