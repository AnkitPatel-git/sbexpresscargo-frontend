info:
  name: Service centers
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/mobile/lookup/service-centers?search="
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.lookup.read
