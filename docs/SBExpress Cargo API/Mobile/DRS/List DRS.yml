info:
  name: List DRS
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/mobile/drs?page=1&limit=20&status=ALL"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.drs.read
