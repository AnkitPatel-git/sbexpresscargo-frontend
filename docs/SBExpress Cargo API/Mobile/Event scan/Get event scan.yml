info:
  name: Get event scan
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/mobile/event-scan/1?eventType=undelivered_received"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: mobile.event_scan.read
