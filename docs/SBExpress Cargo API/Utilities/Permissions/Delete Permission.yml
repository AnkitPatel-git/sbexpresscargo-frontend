info:
  name: Delete Permission
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/utilities/permissions/{{permissionId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Soft-delete permission by id.
