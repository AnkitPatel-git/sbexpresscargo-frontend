info:
  name: Get Role by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/utilities/roles/{{roleId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
