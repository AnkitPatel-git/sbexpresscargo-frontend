info:
  name: Delete Role
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/utilities/roles/{{roleId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
