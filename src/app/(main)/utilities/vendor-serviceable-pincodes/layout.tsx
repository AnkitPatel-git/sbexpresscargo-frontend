import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Vendor Serviceable Pincodes | SB Express Cargo",
}

export default function VendorServiceablePincodesLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return children
}
