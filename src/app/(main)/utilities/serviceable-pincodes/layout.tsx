import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Serviceable Pincodes Master | SB Express Cargo",
}

export default function ServiceablePincodesLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return children
}
